# Generated by stoatworks-unraid/scripts/gen-docker.mjs from fleet.json.
# Repo: caspar-av. Edit fleet.json and regenerate — direct edits are overwritten.
#
# Pull the published image by default. Uncomment build: . to build from this
# checkout instead — useful when testing a change before it reaches GHCR.

services:
  caspar-av:
    image: ghcr.io/stoatworks-labs/caspar-av:latest
    # build: .
    container_name: caspar-av
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config
      - caspar-av-data:/app/data

volumes:
  caspar-av-data:
